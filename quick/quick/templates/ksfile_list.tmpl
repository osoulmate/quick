{% extends 'master.tmpl' %}
{% block content %}
<h1>模板管理</h1>
<hr />
<!-- This is kind weird, but force the name "kickstart"
because we know this is the ksfile page. If this gets moved
to generic_list, then it can be hacked around -->
<ul id="submenubar">
  <li><a class="action" href="/quick/{{ what }}/edit">创建模板</a><li>
</ul>
<table id="listitems" cellspacing="0">
  <thead>
    <tr>
      <th></th>
      <th>文件</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
{% for ksfile,shortname,option in ksfiles %}
    <tr class="{% cycle 'rowodd' 'roweven' %}">
      <td></td>
      <td>{{ shortname }}</td>
  {% if option %}
      <td>
    {% ifequal option "editable" %}<span class="action" onClick="window.location='/quick/ksfile/edit/file:{{ ksfile }}'">编辑</a> {% endifequal %}
    {% ifequal option "viewable" %}<span class="action" onClick="window.location='{{ ksfile }}'>View</a>{% endifequal %}
      </td>
  {% endif %}
    </tr>
{% endfor %}
  </tbody>
</table>
{% endblock content %}
